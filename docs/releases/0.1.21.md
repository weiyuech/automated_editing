完整 Windows x64 安装包，包含后端、FFmpeg、字体和本地模型。

- 修剪镜头超调：云台扫到位后停住、等待、再回摆到目标的这一段，现在归为准备片段，
  镜头在扫动本身结束时收尾，不再带着多余的静止尾巴。
- 同方向的停顿（例如重发命令后继续往前）仍然算同一个镜头，不会被切开。
- 镜头首尾的缓慢漂移（例如停在目标附近慢慢挪 1° 多）也按等待处理。
- 保留 0.1.20 的录制起点时钟对齐，以及之前各版本的就位等待、倍率与唤醒修复。
