完整 Windows x64 安装包，包含后端、FFmpeg、字体和本地模型。

- 停顿阈值统一为 3 秒：云台在扫动中途停住 3 秒以上，无论在不在同一方向、
  有没有到目标，镜头都在停住的位置收尾，之后的部分归为准备片段
  （例如“转到 100° 停一会儿再补到 110°”，现在切在 100°）。
- 短于 3 秒的停顿仍算扫动的一部分，不会被切开。
- 保留行程最远的那一段作为镜头，唤醒时的小幅轻推不会被当成镜头本身。
- 保留 0.1.21 的超调回摆修剪，以及 0.1.20 的录制起点时钟对齐。
