完整 Windows x64 安装包，包含后端、FFmpeg、字体和本地模型。

- 修复回原点准备错误重置倍率的问题：准备阶段到达下一镜头所需的起始位置和倍率，保留真正的变焦动作。
- 保留程序选定的回原点镜头及中途停顿；不会把有效镜头因为终点在原点而当作准备。
- 修复端点式倍率反馈把变焦片段裁成瞬间的问题。反馈只有 1×/2×、不连续或不足时保留完整镜头；可靠反馈才用于保守头尾裁剪。
- 镜头前后等待分别标注并保留所属镜头，与真正的就位准备区分。
- 自动更新原片可用的旧录制区间；保留原片、偏移校准、旧时间树与生成文件，不重编码旧录像，不改写已保存组合。

验证覆盖程序模拟、真实侧车数据回放和自动化检查。未进行真实机器人验证；端点反馈不足时可能仍保留等待，需要原片核对才能确定精确的视觉边界。
